{
  "name": "permissions-workspace",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/adapters/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/adapters/hono build",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/adapters/hono typecheck",
    "test": "bun run --cwd packages/core test && bun run --cwd packages/adapters/hono test",
    "prepublishOnly": "bun run build && bun run typecheck && bun run test"
  }
}
